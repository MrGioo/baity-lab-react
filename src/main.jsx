import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css' 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// Primero usar npm install para instalar las dependencias

// Usar npm run dev para iniciar el proyecto y ver los cambios en tiempo real.