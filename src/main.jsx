import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css' 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// usar npm install para instalar las dependencias del proyecto, incluyendo React y ReactDOM.

// usar npm install axios para instalar la librería axios y poder hacer peticiones HTTP a la API de MockAPI.

// Usar npm run dev para iniciar el proyecto y ver los cambios en tiempo real.

// revisar cambios en mockapi.io para ver las noticias que se han agregado, editado o eliminado desde la aplicación.

